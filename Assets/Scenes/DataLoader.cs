using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataLoader : MonoBehaviour
{


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /*if (Input.anyKeyDown)
        {
            index++;
            Debug.Log(Input.inputString); // this never happens

            var touch_value = Input.inputString == "" ? "Touch" : "Btn";
            Show.text = (touch_value);
        }*/
    }
}
